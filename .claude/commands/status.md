---
description: Phone-friendly project status
---

Give Philip a status report he can read on a phone in under 30 seconds.

1. Current phase and milestone from `docs/ROADMAP.md`.
2. What changed since the last session (check `git log` and `CHANGELOG.md`).
3. Whether `./scripts/check.sh` passes right now — run it.
4. What is blocked on Philip, from `docs/PHILIP-TODO.md`.
5. The single next action.

Keep it to 12 lines or fewer. No preamble. Lead with anything blocking.
